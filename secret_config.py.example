# Copy this file to `secret_config.py` (which is gitignored) and fill in the value below.
#
# The suite needs the Firebase Web API key to refresh your signed-in LIMA session. Keep it
# out of git: use this gitignored file, or set the LIMA_FIREBASE_API_KEY environment
# variable instead.

FIREBASE_API_KEY = "AIza...your Firebase Web API key here..."
