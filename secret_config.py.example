# Copy this file to `secret_config.py` (which is gitignored) and fill in the value below.
#
# The suite needs the Firebase Web API key to refresh your signed-in LIMA session.
#
# Where to find it: Firebase console -> Project Settings -> General -> "Web API Key"
# (ask the LIMA team which Firebase project this suite uses).
#
# Provide it either way (keep it out of git):
#   - fill in FIREBASE_API_KEY below (this file is gitignored), or
#   - set the LIMA_FIREBASE_API_KEY environment variable instead.

FIREBASE_API_KEY = "AIza...your Firebase Web API key here..."
