{
    "_instructions": [
        "Copy this file to lima_config.json (which is gitignored) and set auth_url below.",
        "auth_url is the base URL of the LIMA auth server (a GCP Cloud Run service).",
        "Get the exact URL from the LIMA team, or from the Cloud Run console for the LIMA",
        "project. Keep the real value out of git - it belongs only in your local",
        "lima_config.json, never in this committed example."
    ],
    "auth_url": "https://YOUR-LIMA-AUTH-SERVER-URL"
}
